---
deployment:
  tasks:
    - export DEPLOYPATH=/home/sarum964/public_html/
    - /bin/mkdir -p $DEPLOYPATH
    - /bin/rsync -avz --exclude '.git*' --exclude '.cpanel.yml' --exclude 'vendor/mailjet/' ./ $DEPLOYPATH